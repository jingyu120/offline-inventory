### 21. Real-Data Schema & Catalog Refinements (Conforming to Master Sheets)

- **Condition Grading Support:** Update the `InteractionItem` database table layout to include a `stock_condition` property string enum supporting `GOOD`, `BAD`, and `WET`. Ensure frontend selectors default to `GOOD` but keep values editable so field reps can flag custom clearance sales on damaged stock items.
- **Granular Product Variant Sub-Types:** Update item catalog schema objects to parse attribute suffix arrays cleanly. Split lengthy text listings into structured relationship objects containing explicit columns for color properties, material sub-types (e.g., RE vs. MR), and hardware finishes (e.g., CP vs. BL).
- **Supply-Deficit Warning Banners:** Add a binary boolean flag on the core backend item framework table layout called `is_in_deficit`. If toggled true by administrators, display a clear warning indicator sub-text element next to that item on the rep's checkout selector drawer to prevent unrealistic delivery timelines for backordered goods.
- **Commercial Project Allocation Mode:** Create a `Project` database model table linked directly to your interaction logger schema layouts. Allow sales representatives to easily categorize bulk contract sales orders under specific construction identifiers (e.g., Galaxy Tower-3, Zaw Residence) to enable granular corporate contract analytics for management.
