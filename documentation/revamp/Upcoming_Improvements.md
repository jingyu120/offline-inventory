Conflict Resolution Rule is Still Row-Level Last-Write-Wins: In your applyPullChanges engine function, the incoming sync server changes are evaluated block-by-block. For updates, it applies the whole incoming record row object directly via .set(record). If your plans call for true property-specific delta patching (merging different text updates on the same shop record row), the agent will eventually need to update this logic block to unpack specific field keys rather than swapping the whole entity row.

Fulfillment Inventory Schema Framework is Present but Ready for Separation: Your schema now correctly structures the table frameworks for stock_locations and stock_balances. Since we agreed on a Sales-First operational deployment, ensure your app components are currently ignoring these storage fields during checkout routines. Let them act as passive data containers until the management analytics loop is 100% stable in production.

Image Database Flag Reset Strategy: When an offline image successfully uploads to the cloud bucket, the queue engine modifies the parent row entry inside interaction_logs by writing the viber_screenshot_url and resetting synced_at_server: null. This is an incredibly smart pattern—it explicitly flags the parent text log as "updated offline," which automatically forces your primary syncData() engine to pick it up on the next push loop and send the fresh URL up to management.
